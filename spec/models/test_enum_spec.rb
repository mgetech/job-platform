# spec/models/test_enum_spec.rb
require 'rails_helper'

RSpec.describe TestEnum, type: :model do
  it 'should load without error' do
    expect(true).to be_truthy # Just a simple assertion to ensure the file loads
  end
end